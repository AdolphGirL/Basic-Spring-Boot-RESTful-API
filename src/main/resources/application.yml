spring:
  profiles:
    active:
    - dev
    - prod

server:
  servlet:
    context-path: /api
    
---

spring:
  profiles: dev
  
server:
  port: 8080
  
---

spring:
  profiles: prod
  
server:
  port: 8081
